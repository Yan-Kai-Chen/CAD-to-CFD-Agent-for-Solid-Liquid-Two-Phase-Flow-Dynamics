# Surface Cleanup Example

This template audits imported geometry to distinguish mesh bodies, graphics bodies, and standard BREP bodies with many real topology edges.

Place private geometry locally under an ignored `input` folder before running the script.
