"""Minimal MCP server placeholder.

The public v0.2.0 release focuses on the Python SolidWorks automation layer,
the common CAD backend contract, and the Siemens NX controlled-journal backend.
The production MCP server should expose only safe, high-level tools and must not
expose arbitrary code execution.
"""


def main() -> int:
    raise SystemExit("FromCAD2CFD SolidWorks MCP server is a roadmap placeholder in v0.2.0.")


if __name__ == "__main__":
    main()
