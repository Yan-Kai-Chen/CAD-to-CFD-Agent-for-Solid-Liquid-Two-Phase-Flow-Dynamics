"""Minimal MCP server placeholder.

The public v0.1.0 release focuses on the Python SolidWorks automation layer.
The production MCP server should expose only safe, high-level tools and must not
expose arbitrary code execution.
"""


def main() -> int:
    raise SystemExit("FromCAD2CFD SolidWorks MCP server is a roadmap placeholder in v0.1.0.")


if __name__ == "__main__":
    main()
