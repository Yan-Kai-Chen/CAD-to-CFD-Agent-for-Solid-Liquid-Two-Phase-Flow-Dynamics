# Project-level Codex MCP config draft for the FromCAD2CFD postprocessing server.
# Install the package first:
#   python -m pip install -e ".[mcp]"

[mcp_servers.fromcad2cfd_postprocessing]
command = "python"
args = ["-m", "fromcad2cfd_mcp_postprocessing.server"]
cwd = "<REPO_ROOT>"
